{
  "name": "deprecation-crawler",
  "version": "0.0.0-development",
  "description": "",
  "main": "index.js",
  "bin": "cli.js",
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build": "tsc",
    "postbuild": "cpy ./package.json ./dist && cpy ./cli.js ./dist",
    "local": "ts-node lib/local.ts",
    "build-and-pack": "npm run build && cd dist && npm pack && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "enquirer": "^2.3.5",
    "prettier": "^2.0.5",
    "ts-morph": "^7.1.0",
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "@types/prettier": "^2.0.2",
    "@types/node": "^13.5.3",
    "cpy-cli": "^3.1.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.1",
    "typescript": "^3.7.5"
  }
}
